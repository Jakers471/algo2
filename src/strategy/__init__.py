"""src.strategy — decision logic (entries/exits).

Placeholder. Strategies consume src.indicators values and emit signals/orders
through the broker interface (src.brokers.base.Broker) — never through
broker-specific code.
"""
