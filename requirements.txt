flask>=3.0      # backend API + static host
pandas>=2.0     # data crunching (parquet I/O, resampling)
numpy>=1.24     # indicator math (src/indicators)
pyarrow>=14.0   # parquet engine for pandas
pyyaml>=6.0     # reads algo_config.yaml (the knob config)
pywebview>=4.4  # wraps it in a native desktop window
